<Window x:Class="MicForwarderWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="iPhone Mic Forwarder" Height="250" Width="400"
        Closing="Window_Closing">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Select Output Device (e.g. VB-Cable Input)" FontWeight="Bold" Margin="0,0,0,5" Grid.Row="0"/>
        
        <ComboBox x:Name="DeviceComboBox" Grid.Row="1" Margin="0,0,0,15" DisplayMemberPath="Name"/>
        
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
            <Button x:Name="ConnectButton" Content="Connect to iPhone" Width="150" Height="40" Click="ConnectButton_Click"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Text="Ready. Make sure iPhone app is running." 
                   Grid.Row="3" VerticalAlignment="Bottom" HorizontalAlignment="Center" 
                   Foreground="Gray" Margin="0,10,0,0"/>
    </Grid>
</Window>
