<Window x:Class="MicForwarderWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="iPhone Mic Forwarder" Height="350" Width="400"
        Closing="Window_Closing">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Select Output Device (e.g. VB-Cable Input)" FontWeight="Bold" Margin="0,0,0,5" Grid.Row="0"/>
        
        <ComboBox x:Name="DeviceComboBox" Grid.Row="1" Margin="0,0,0,10" DisplayMemberPath="Name"/>
        
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Transport Mode" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox x:Name="TransportComboBox" SelectionChanged="TransportComboBox_SelectionChanged">
                <ComboBoxItem Content="USB (TCP - Ultra Low Latency)" IsSelected="True"/>
                <ComboBoxItem Content="Wi-Fi (UDP - Wireless)"/>
            </ComboBox>
            <StackPanel x:Name="WifiPanel" Visibility="Collapsed" Margin="0,10,0,0">
                <TextBlock Text="iPhone Wi-Fi IP Address (e.g. 192.168.1.50)" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="IpAddressTextBox" Text="192.168.1." Padding="2"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center">
            <Button x:Name="ConnectButton" Content="Connect to iPhone" Width="150" Height="40" Click="ConnectButton_Click"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Text="Ready. Make sure iPhone app is running." 
                   Grid.Row="4" VerticalAlignment="Bottom" HorizontalAlignment="Center" 
                   Foreground="Gray" Margin="0,10,0,0"/>
    </Grid>
</Window>
